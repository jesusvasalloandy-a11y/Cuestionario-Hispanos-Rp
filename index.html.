<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hispanos RP | Postulación</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            background-color: #0a0a0a;
            background-image: linear-gradient(rgba(0, 0, 0, 0.8), rgba(0, 0, 0, 0.8)), 
                              url('https://images.unsplash.com/photo-1614850523296-d8c1af93d400?auto=format&fit=crop&q=80&w=1920');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            color: white;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
        }
        .logo { width: 180px; margin-top: 30px; filter: drop-shadow(0 0 15px #bc13fe); }
        form {
            background: rgba(20, 20, 30, 0.95);
            padding: 30px;
            border-radius: 15px;
            border: 1px solid #bc13fe;
            width: 100%;
            max-width: 500px;
            margin-top: 25px;
            box-shadow: 0 0 25px rgba(188, 19, 254, 0.2);
        }
        h1 { font-size: 1.8rem; margin-top: 15px; text-shadow: 0 0 10px #bc13fe; }
        h2 { color: #bc13fe; border-bottom: 1px solid #333; padding-bottom: 10px; font-size: 1.2rem; }
        .campo { margin-bottom: 20px; }
        label { display: block; margin-bottom: 8px; font-weight: bold; font-size: 0.9rem; }
        input, textarea {
            width: 100%;
            padding: 12px;
            border-radius: 8px;
            border: 1px solid #444;
            background: #000;
            color: white;
            box-sizing: border-box;
            outline: none;
        }
        input:focus, textarea:focus { border-color: #bc13fe; }
        button {
            width: 100%;
            padding: 15px;
            background: #bc13fe;
            border: none;
            color: white;
            font-weight: bold;
            border-radius: 8px;
            cursor: pointer;
            font-size: 16px;
            transition: 0.3s;
        }
        button:hover { background: #9b07d4; transform: scale(1.02); }
    </style>
</head>
<body>

    <img src="https://i.ibb.co/XfqyvH6W/IMG-5690.png" class="logo" alt="Logo Hispanos RP">
    <h1>Postulación Hispanos RP</h1>

    <form id="quizForm">
        <h2>Información General</h2>
        <div class="campo">
            <label>Nombre de Usuario (Discord):</label>
            <input type="text" placeholder="Ej: Juanito#1234" required>
        </div>
        
        <div class="campo">
            <label>¿Qué edad tienes?</label>
            <input type="number" placeholder="Tu edad" required>
        </div>

        <h2>Conceptos de Rol</h2>
        <div class="campo">
            <label>¿Qué es para ti el Powergaming (PG)?</label>
            <textarea rows="3" placeholder="Define el concepto..."></textarea>
        </div>

        <div class="campo">
            <label>¿Por qué quieres entrar al servidor?</label>
            <textarea rows="3" placeholder="Cuéntanos tus motivos..."></textarea>
        </div>

        <button type="submit">Enviar Postulación</button>
    </form>

</body>
</html>
